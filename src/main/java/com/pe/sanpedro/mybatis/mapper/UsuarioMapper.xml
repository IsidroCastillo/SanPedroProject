<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pe.sanpedro.mybatis.mapper.UsuarioMapper">

	<select id="login" parameterType="java.util.Map" resultType="Usuario">
		select idUsuario,usuario,password,id_tipo_usuario,fechaCreacion,estado
		 from usuario where usuario = #{user} and password = #{pwd}
	</select>
	
</mapper>